<Window x:Class="Vamt_for_us.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Vamt_for_us" 
        xmlns:viewmodels="clr-namespace:Vamt_for_us.ViewModels"
        mc:Ignorable="d"
        Title="VAMT for Us" 
        Height="700" 
        Width="1000" 
        Icon="/Icon.png"
        WindowStartupLocation="CenterScreen"
        Background="#FF0F0F0F"
        FontFamily="Segoe UI">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <!-- Тёмная цветовая палитра с розово-фиолетовыми акцентами -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FF0F0F0F"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FF1A1A1A"/>
        <SolidColorBrush x:Key="CardHoverBrush" Color="#FF252525"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#FF333333"/>
        <SolidColorBrush x:Key="BorderHoverBrush" Color="#FF444444"/>

        <!-- Розово-фиолетовые акценты -->
        <SolidColorBrush x:Key="AccentPrimaryBrush" Color="#FFA000FF"/>
        <SolidColorBrush x:Key="AccentSecondaryBrush" Color="#FF8927B0"/>
        <SolidColorBrush x:Key="AccentTertiaryBrush" Color="#FF9768C8"/>
        <SolidColorBrush x:Key="AccentLightBrush" Color="#4CA421F3"/>

        <!-- Текст -->
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFFFF"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#FFB0B0B0"/>
        <SolidColorBrush x:Key="TextTertiaryBrush" Color="#FF888888"/>
        <SolidColorBrush x:Key="TextAccentBrush" Color="#FF7D00FF"/>

        <!-- Состояния -->
        <SolidColorBrush x:Key="HoverBrush" Color="#15FFFFFF"/>
        <SolidColorBrush x:Key="PressedBrush" Color="#25FFFFFF"/>
        <SolidColorBrush x:Key="SelectionBrush" Color="#30E100FF"/>

        <!-- Статусы -->
        <SolidColorBrush x:Key="SuccessBrush" Color="#FF5B9C5D"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#FFEA5E54"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FFD4A053"/>
        <SolidColorBrush x:Key="OnlineBrush" Color="#FF5B9C5D"/>
        <SolidColorBrush x:Key="OfflineBrush" Color="#FFEA5E54"/>

        <!-- Стиль для прозрачных RepeatButton -->
        <Style x:Key="TransparentRepeatButton" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsHitTestVisible" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кастомного скроллбара -->
        <Style x:Key="ModernScrollBar" TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="GridRoot" 
                              Width="{TemplateBinding Width}" 
                              Background="{TemplateBinding Background}">

                            <Track x:Name="PART_Track" 
                                   IsDirectionReversed="true">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand" 
                                                  Style="{StaticResource TransparentRepeatButton}"/>
                                </Track.DecreaseRepeatButton>

                                <Track.Thumb>
                                    <Thumb x:Name="Thumb">
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Background" Value="{StaticResource BorderBrush}"/>
                                                <Setter Property="BorderBrush" Value="Transparent"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border x:Name="thumbBorder" 
                                                                    Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="1"
                                                                    CornerRadius="4"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource AccentSecondaryBrush}"/>
                                                                </Trigger>
                                                                <Trigger Property="IsDragging" Value="True">
                                                                    <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource AccentPrimaryBrush}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>

                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand" 
                                                  Style="{StaticResource TransparentRepeatButton}"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Orientation" Value="Horizontal">
                                <Setter Property="Height" Value="8"/>
                                <Setter Property="Width" Value="Auto"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для ScrollViewer с кастомным скроллбаром -->
        <Style x:Key="ModernScrollViewer" TargetType="ScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    Grid.Column="0"
                                                    Grid.Row="0"
                                                    Margin="{TemplateBinding Padding}"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Grid.Column="1"
                                       Grid.Row="0"
                                       Style="{StaticResource ModernScrollBar}"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Grid.Column="0"
                                       Grid.Row="1"
                                       Style="{StaticResource ModernScrollBar}"
                                       Orientation="Horizontal"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Анимированные стили для кнопок -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter x:Name="contentPresenter"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Наведение - плавное увеличение и смена фона -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Duration="0:0:0.15" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleX"
                                                            To="1.03"/>
                                            <DoubleAnimation Duration="0:0:0.15" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleY"
                                                            To="1.03"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Duration="0:0:0.15" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleX"
                                                            To="1"/>
                                            <DoubleAnimation Duration="0:0:0.15" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleY"
                                                            To="1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>

                            <!-- Нажатие - уменьшение и темный фон -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PressedBrush}"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Duration="0:0:0.08" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleX"
                                                            To="0.97"/>
                                            <DoubleAnimation Duration="0:0:0.08" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleY"
                                                            To="0.97"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Duration="0:0:0.08" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleX"
                                                            To="1"/>
                                            <DoubleAnimation Duration="0:0:0.08" 
                                                            Storyboard.TargetName="scaleTransform"
                                                            Storyboard.TargetProperty="ScaleY"
                                                            To="1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource AccentPrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentPrimaryBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentSecondaryBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentSecondaryBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentTertiaryBrush}"/>
                    <Setter Property="BorderBrush" Value="{StaticResource AccentTertiaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для современных CheckBox -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid x:Name="root">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Внешний border чекбокса -->
                            <Border x:Name="checkBoxBorder"
                                    Grid.Column="0"
                                    Width="{TemplateBinding Width}"
                                    Height="{TemplateBinding Height}"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="2">

                                <!-- Галочка -->
                                <Path x:Name="checkMark"
                                      Data="M 3,8 L 6,11 L 13,4"
                                      Stroke="{StaticResource TextPrimaryBrush}"
                                      StrokeThickness="2"
                                      Stretch="Fill"
                                      Margin="1"
                                      Opacity="0"/>
                            </Border>

                            <!-- Контент (текст) -->
                            <ContentPresenter x:Name="contentPresenter"
                                              Grid.Column="1"
                                              Margin="8,0,0,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              RecognizesAccessKey="True"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Checked состояние -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Opacity" Value="1"/>
                                <Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource AccentPrimaryBrush}"/>
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource AccentPrimaryBrush}"/>
                            </Trigger>

                            <!-- MouseOver состояние -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource AccentSecondaryBrush}"/>
                                <Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>

                            <!-- Pressed состояние -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource PressedBrush}"/>
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource AccentPrimaryBrush}"/>
                            </Trigger>

                            <!-- Disabled состояние -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource BorderBrush}"/>
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                <Setter TargetName="checkMark" Property="Stroke" Value="{StaticResource TextTertiaryBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
                            </Trigger>

                            <!-- Disabled и Checked -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsEnabled" Value="False"/>
                                    <Condition Property="IsChecked" Value="True"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource BorderBrush}"/>
                                <Setter TargetName="checkMark" Property="Stroke" Value="{StaticResource TextTertiaryBrush}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComputerCheckBox" TargetType="CheckBox" BasedOn="{StaticResource ModernCheckBox}">
            <Setter Property="Margin" Value="0,0,12,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                          Margin="{TemplateBinding Padding}"
                                          Background="Transparent"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="ScrollViewer"
                                          Style="{StaticResource ModernScrollViewer}"
                                          Focusable="False"
                                          Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- Стиль для элементов списка ключей -->
        <Style x:Key="KeyItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectionBrush}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentPrimaryBrush}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для элементов списка компьютеров -->
        <Style x:Key="ComputerItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стили для TabControl -->
        <Style x:Key="ModernTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TabPanel x:Name="HeaderPanel"
                                      Grid.Row="0"
                                      Panel.ZIndex="1" 
                                      Margin="0,0,0,-1"
                                      IsItemsHost="True"
                                      KeyboardNavigation.TabIndex="1"
                                      Background="Transparent"/>

                            <Border x:Name="ContentPanel"
                                    Grid.Row="1"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    KeyboardNavigation.TabNavigation="Local"
                                    KeyboardNavigation.DirectionalNavigation="Contained"
                                    KeyboardNavigation.TabIndex="2">
                                <ContentPresenter x:Name="PART_SelectedContentHost"
                                                  ContentSource="SelectedContent"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid x:Name="Root">
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Margin="0,0,-1,0">
                                <ContentPresenter x:Name="ContentSite"
                                                  ContentSource="Header"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentPrimaryBrush}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="0,0,0,2"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardHoverBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource BorderBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextTertiaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="12" Background="{StaticResource BackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <Border Grid.Row="0" 
                Background="{StaticResource CardBackgroundBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                Padding="12"
                Margin="0,0,0,12">
            <StackPanel Orientation="Horizontal">
                <Button Content="Информация" 
                        Click="InfoButton_Click" 
                        Style="{StaticResource ModernButton}"/>
                <Button Content="Обновить" 
                        Click="ReloadButton_Click"
                        Style="{StaticResource ModernButton}"/>
                <Button Content="Экспорт ключей" 
                        Click="ExportLicenseReport_Click"
                        Style="{StaticResource ModernButton}"/>
                <Button Content="Экспорт активаций" 
                        Click="ExportActivatedReport_Click"
                        Style="{StaticResource ModernButton}"/>
                <Button Content="Настройки" 
                        Click="SettingsButton_Click"
                        Style="{StaticResource ModernButton}"/>
            </StackPanel>
        </Border>

        <!-- Прогресс бар -->
        <ProgressBar Grid.Row="1" 
                     Height="2" 
                     IsIndeterminate="{Binding IsLoading}" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisConverter}}"
                     Margin="0,0,0,12"
                     Background="{StaticResource BorderBrush}"
                     Foreground="{StaticResource AccentPrimaryBrush}"/>

        <!-- Вкладки -->
        <TabControl Grid.Row="2" 
                    Style="{StaticResource ModernTabControl}"
                    Background="Transparent">

            <!-- Вкладка 1: Ключи продуктов -->
            <TabItem Header="Ключи продуктов" Style="{StaticResource ModernTabItem}">
                <Grid Background="{StaticResource BackgroundBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>

                    <!-- Список ключей продуктов -->
                    <ListBox x:Name="ProductKeysListBox"
                             Grid.Row="0"
                             Style="{StaticResource ModernListBox}"
                             ItemContainerStyle="{StaticResource KeyItemStyle}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             SelectionChanged="ProductKeysListBox_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" MinWidth="200"/>
                                        <ColumnDefinition Width="Auto" MinWidth="70"/>
                                        <ColumnDefinition Width="Auto" MinWidth="120"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Ключ и описание -->
                                    <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,12,0">
                                        <TextBlock Text="{Binding KeyValue}" 
                                                   FontWeight="SemiBold"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextPrimaryBrush}"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding KeyDescription}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   Margin="0,2,0,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <StackPanel Orientation="Vertical">
                                            <TextBlock Text="Осталось:" 
                                                     Foreground="{StaticResource TextSecondaryBrush}"
                                                        FontSize="10"
                                                             Margin="0,0,4,0"/>
                                            
                                            <!-- Комментарий -->
                                            <TextBlock Grid.Column="2" 
                                                    Text="{Binding UserRemarks}"
                                                Foreground="{StaticResource TextTertiaryBrush}"
                                                        FontSize="10"
                                                                VerticalAlignment="Center"
                                                        TextTrimming="CharacterEllipsis"
                                                                ToolTip="{Binding UserRemarks}"/>
                                            <Border Grid.Column="1" 
                                             Background="{StaticResource CardHoverBrush}" 
                                                CornerRadius="4"
                                                Padding="6,2"
                                                VerticalAlignment="Top"
                                                 HorizontalAlignment="Left"
                                                 Margin="0,0,8,0">
                                                <TextBlock Text="{Binding RemainingActivations}" 
                                                    FontWeight="Bold"
                                                     FontSize="11"
                                                    Foreground="{StaticResource AccentPrimaryBrush}"/>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Активации 
                                    -->

                                    
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Разделитель -->
                    <GridSplitter Grid.Row="1" 
                                  Height="1" 
                                  HorizontalAlignment="Stretch" 
                                  Background="{StaticResource BorderBrush}"
                                  ResizeDirection="Rows"/>

                    <!-- Детали выбранного ключа -->
                    <GroupBox Grid.Row="2" 
                              Header="Детали выбранного ключа"
                              x:Name="ProductKeyDetailsGroup"
                              Style="{StaticResource ModernGroupBox}" 
                              BorderThickness="0" 
                              Visibility="Collapsed">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Ключ -->
                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                       Text="Ключ:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" 
                                       x:Name="SelectedKeyText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       VerticalAlignment="Center"/>
                            <Button Grid.Row="0" Grid.Column="2" 
                                    Content="Копировать" 
                                    Click="CopyProductKey_Click"
                                    Style="{StaticResource ModernButton}"
                                    Width="80"
                                    HorizontalAlignment="Right"/>

                            <!-- Описание -->
                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                       Text="Описание:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" 
                                       x:Name="SelectedDescriptionText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       Grid.ColumnSpan="2"
                                       VerticalAlignment="Center"/>

                            <!-- Активаций -->
                            <TextBlock Grid.Row="2" Grid.Column="0" 
                                       Text="Активаций:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" 
                                       x:Name="SelectedActivationsText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       Grid.ColumnSpan="2"
                                       VerticalAlignment="Center"/>

                            <!-- Комментарий -->
                            <TextBlock Grid.Row="3" Grid.Column="0" 
                                       Text="Комментарий:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,5,8,0"
                                       VerticalAlignment="Top"/>
                            <TextBox Grid.Row="3" Grid.Column="1" 
                                     x:Name="CommentTextBox"
                                     Style="{StaticResource ModernTextBox}"
                                     Margin="0,5,8,0"
                                     VerticalScrollBarVisibility="Auto"
                                     AcceptsReturn="True"
                                     TextWrapping="Wrap"
                                     MinHeight="60"
                                     VerticalAlignment="Stretch"
                                     KeyDown="CommentTextBox_KeyDown"/>
                            <StackPanel Grid.Row="3" Grid.Column="2" 
                                        Margin="8,5,0,0"
                                        VerticalAlignment="Top">
                                <Button Content="Сохранить" 
                                        Click="SaveCommentButton_Click"
                                        Style="{StaticResource AccentButton}"
                                        Margin="0,0,0,4"
                                        Width="80"/>
                                <TextBlock Text="Ctrl+Enter" 
                                           FontSize="9" 
                                           Foreground="{StaticResource TextTertiaryBrush}" 
                                           HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </GroupBox>

                    <TextBlock Grid.Row="2" 
                               Text="Выберите ключ из списка для просмотра деталей" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               FontStyle="Italic"
                               Foreground="{StaticResource TextTertiaryBrush}"
                               x:Name="NoSelectionProductKeyText"/>
                </Grid>
            </TabItem>

            <!-- Вкладка 2: Активированные продукты -->
            <TabItem Header="Активированные продукты" Style="{StaticResource ModernTabItem}">
                <Grid Background="{StaticResource BackgroundBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="200"/>
                    </Grid.RowDefinitions>

                    <!-- Список активированных продуктов -->
                    <ListBox x:Name="ActiveProductsListBox"
                             Grid.Row="0"
                             Style="{StaticResource ModernListBox}"
                             ItemContainerStyle="{StaticResource KeyItemStyle}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             SelectionChanged="ActiveProductsListBox_SelectionChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Orientation="Vertical">
                                        <TextBlock Text="{Binding FullyQualifiedDomainName}" 
                                                   FontWeight="SemiBold"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding ProductKeyId}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding LicenseStatusText}" 
                                                   FontSize="11"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding KeyTypeName}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="10"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Разделитель -->
                    <GridSplitter Grid.Row="1" 
                                  Height="1" 
                                  HorizontalAlignment="Stretch" 
                                  Background="{StaticResource BorderBrush}"
                                  ResizeDirection="Rows"/>

                    <!-- Детали выбранного продукта -->
                    <GroupBox Grid.Row="2" 
                              Header="Детали выбранного продукта"
                              x:Name="ActiveProductDetailsGroup"
                              Style="{StaticResource ModernGroupBox}"
                              Visibility="Collapsed" BorderThickness="0,0,0,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" 
                                       Text="Доменное имя:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" 
                                       x:Name="SelectedDomainText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"/>
                            <Button Grid.Row="0" Grid.Column="2" 
                                    Content="Копировать" 
                                    Click="CopyDomain_Click"
                                    Style="{StaticResource ModernButton}"
                                    Width="80"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" 
                                       Text="Ключ продукта:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" 
                                       x:Name="SelectedProductKeyText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       Grid.ColumnSpan="2"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" 
                                       Text="Статус лицензии:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" 
                                       x:Name="SelectedLicenseStatusText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,8"
                                       Grid.ColumnSpan="2"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" 
                                       Text="Тип ключа:" 
                                       FontWeight="SemiBold" 
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,0"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" 
                                       x:Name="SelectedKeyTypeText"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       Margin="0,0,8,0"
                                       Grid.ColumnSpan="2"/>
                        </Grid>
                    </GroupBox>

                    <TextBlock Grid.Row="2" 
                               Text="Выберите продукт из списка для просмотра деталей" 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               FontStyle="Italic"
                               Foreground="{StaticResource TextTertiaryBrush}"
                               x:Name="NoSelectionActiveProductText"/>
                </Grid>
            </TabItem>

            <!-- Вкладка 3: Компьютеры домена -->
            <TabItem Header="Компьютеры домена" Style="{StaticResource ModernTabItem}">
                <Grid Background="{StaticResource BackgroundBrush}" Margin="0,8,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Панель управления -->
                    <Border Grid.Row="0" 
                            Background="{StaticResource CardBackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            Padding="12"
                            Margin="0,0,0,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Content="Обновить список" 
                                    Command="{Binding GetAllComputersInDomainCommand}"
                                    Style="{StaticResource AccentButton}"/>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock Text="Всего:" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding DomainComputers.Count}" 
                                           FontWeight="SemiBold"
                                           Foreground="{StaticResource TextPrimaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>

                                <TextBlock Text="Онлайн:" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding OnlineComputersCount}" 
                                           Foreground="{StaticResource OnlineBrush}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>

                                <TextBlock Text="Офлайн:" 
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,4,0"/>
                                <TextBlock Text="{Binding OfflineComputersCount}" 
                                           Foreground="{StaticResource OfflineBrush}"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Список компьютеров -->
                    <ListBox Grid.Row="1" 
                             ItemsSource="{Binding DomainComputers}"
                             Style="{StaticResource ModernListBox}"
                             ItemContainerStyle="{StaticResource ComputerItemStyle}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <CheckBox Grid.Column="0" 
                                              IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              IsEnabled="{Binding IsOnline}"
                                              Style="{StaticResource ComputerCheckBox}"/>

                                    <StackPanel Grid.Column="1" Orientation="Vertical">
                                        <TextBlock Text="{Binding Name}" 
                                                   FontWeight="SemiBold"
                                                   FontSize="13"
                                                   Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding IPAddress}" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   FontSize="11"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Ellipse Width="8" Height="8" 
                                               Fill="{Binding StatusColor}" 
                                               Margin="0,0,6,0"/>
                                        <TextBlock Text="{Binding Status}" 
                                                   Foreground="{Binding StatusColor}"
                                                   FontWeight="SemiBold"
                                                   FontSize="11"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Панель отправки сообщений -->
                    <Border Grid.Row="2" 
                            Background="{StaticResource CardBackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            Padding="12"
                            Margin="0,12,0,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Сообщение:"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>
                            <TextBox Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                                     Style="{StaticResource ModernTextBox}"
                                     Width="300"
                                     Margin="0,0,12,0"/>
                            <Button Content="Отправить выделенным"
                                    Command="{Binding SendMessageCommand}"
                                    Style="{StaticResource AccentButton}"/>
                        </StackPanel>
                    </Border>

                    <!-- Статусная строка -->
                    <Border Grid.Row="3" 
                            Background="{StaticResource CardBackgroundBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            Padding="8">
                        <TextBlock Text="{Binding StatusMessage}"
                                   Foreground="{Binding StatusColor}"
                                   FontSize="11"
                                   HorizontalAlignment="Center"/>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <!-- Настройки -->
        <GroupBox x:Name="SettingsGroupBox"
                  Grid.Row="3"
                  Header="Настройки подключения к БД"
                  Style="{StaticResource ModernGroupBox}"
                  Visibility="Collapsed"
                  Margin="0,12,0,0" BorderThickness="0,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                           Text="Строка подключения к базе данных:"
                           FontWeight="SemiBold" 
                           Foreground="{StaticResource TextPrimaryBrush}"
                           Margin="0,0,0,8"/>

                <TextBox Grid.Row="1" Grid.ColumnSpan="2"
                         x:Name="ConnectionStringTextBox"
                         Style="{StaticResource ModernTextBox}"
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="10"
                         MinHeight="60"
                         Margin="0,0,0,12"/>

                <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                    <Button Content="Проверить подключение" 
                            Click="TestConnection_Click"
                            Style="{StaticResource ModernButton}"/>
                    <Button Content="Сбросить" 
                            Click="ResetConnection_Click"
                            Style="{StaticResource ModernButton}"
                            Margin="8,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Сохранить" 
                            Click="SaveConnection_Click"
                            Style="{StaticResource AccentButton}"
                            Margin="0,0,8,0"/>
                    <Button Content="Отмена" 
                            Click="CancelSettings_Click"
                            Style="{StaticResource ModernButton}"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Window>